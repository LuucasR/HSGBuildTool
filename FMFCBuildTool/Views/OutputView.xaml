<UserControl x:Class="FMFCBuildTool.Views.OutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:FMFCBuildTool.Views">

    <!--
        The full-screen log is the same control and the same view-model as the bottom
        dock. Previously these were two separate renderers of the same stream with
        different colouring rules and different filters.
    -->
    <views:LogPanel/>

</UserControl>
